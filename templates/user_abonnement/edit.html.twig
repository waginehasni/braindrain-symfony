{% extends 'basefront.html.twig' %}

{% block title %}Edit Abonnement{% endblock %}

{% block body %}
    {% block content %}
<div class="container">
        <h1>Edit Abonnement</h1>

    {{ include('user_abonnement/_form.html.twig', {'button_label': 'Modifier'}) }}
        <ul class="list-inline mb-0">
            <li class="list-inline-item mr-3">
                <p class="text-muted mb-0"><a href="{{ path('user_abonnement_index') }}" class="btn btn-sm btn-info">back to list</a> </p>
            </li>

            <li class="list-inline-item">
                <p class="text-muted mb-0">{{ include('user_abonnement/_delete_form.html.twig') }}
                </p>
            </li>
        </ul>
</div>>

{% endblock %}
{% endblock %}

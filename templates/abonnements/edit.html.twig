{% extends 'base.html.twig' %}

{% block title %}Edit Abonnement{% endblock %}

{% block body %}
    <div class="container">
    <h1>Modifier Abonnement</h1>

    {{ include('abonnements/_form.html.twig', {'button_label': 'Update'}) }}

<br>
    <ul class="list-inline mb-0">
        <li class="list-inline-item mr-3">
            <p class="text-muted mb-0"><a href="{{ path('abonnements_index') }}" class="btn btn-sm btn-info">back to list</a> </p>
        </li>

        <li class="list-inline-item">
            <p class="text-muted mb-0">{{ include('abonnements/_delete_form.html.twig') }}
            </p>
        </li>
    </ul>
    </div>
{% endblock %}
